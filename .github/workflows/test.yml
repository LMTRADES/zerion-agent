name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        node-version: [20]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        run: |
          node --test tests/unit/agent-engine.test.mjs
          node --test tests/unit/cli-and-final.test.mjs
          node --test tests/unit/final-batch.test.mjs
          node --test tests/unit/final-push.test.mjs
          node --test tests/unit/privacy.test.mjs
          node --test tests/unit/final-edge-push.test.mjs
          node --test tests/unit/final-expansion.test.mjs
          node --test tests/unit/thousand-final.test.mjs
